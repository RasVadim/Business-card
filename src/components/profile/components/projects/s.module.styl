.wrapper
  margin-top 48px
  display flex
  flex-direction column
  gap 28px

.sectionTitle
  font-size 20px
  font-weight 700
  color var(--opposite-contrast-color)
  padding 8px 0

.list
  display grid
  grid-template-columns 1fr
  gap 20px