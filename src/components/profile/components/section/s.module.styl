.section
  background var(--background-front-color)
  border-radius 24px
  padding 16px
  box-shadow 0 1px 2px rgba(0, 0, 0, 0.06)

.dark
  background var(--opposite-contrast-color)

.sectionTitle
  margin 0 0 12px
  font-weight 700
  color var(--text-color)