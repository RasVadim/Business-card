.content
  min-height 100vh
  background var(--background-color)
  background-image linear-gradient(45deg, var(--background-front-color), var(--background-color))
  overflow-y auto
  overflow-x hidden